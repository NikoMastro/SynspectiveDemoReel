# Synspective sample product + format manual — local only, never committed
data/
# Derived from that product, so it is not redistributed either.
# Regenerate by running notebooks/03-tle-ground-track.ipynb with your own copy of the sample.
fixtures/precise_ephemeris.json

# Go
bin/
*.test
coverage.out

# Generated from proto/ — run `make generate`
backend/gen/
frontend/src/gen/

# Node
node_modules/
frontend/dist/
.vite/

# Python
.venv/
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Playwright
test-results/
playwright-report/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
# Saved plans can embed resolved variable values, and they are never worth keeping.
tfplan
*.tfplan

# Local config / credentials
.env
.env.local
*-service-account.json
gha-creds-*.json

# Editors / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
