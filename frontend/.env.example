# Base URL of the Go scene-service REST API.
# Left unset, the app calls /api/v1 on its own origin, which the Vite dev server
# proxies to http://localhost:8080 (see vite.config.ts).
VITE_API_BASE=/api/v1
